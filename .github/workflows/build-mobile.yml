name: Build BlooRush Mobile Apps (Android & iOS)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-android:
    name: 🤖 Build Android APK
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Check out repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: 📱 Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: 🟢 Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: 📦 Install Root Dependencies
        run: npm install

      - name: 📦 Install Client App Dependencies
        run: |
          cd apps/client
          npm install

      - name: 🔄 Compile Web Assets & Sync Native Android
        run: |
          node apps/client/scripts/build.js
          cd apps/client
          npx cap sync android

      - name: 🔑 Make Gradlew Executable
        run: chmod +x apps/client/android/gradlew

      - name: 🔨 Compile Client Android Debug APK
        run: |
          cd apps/client/android
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: 📤 Upload Client Android APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: BlooRush-Client-Android-APK
          path: apps/client/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30

  build-ios:
    name: 🍎 Build iOS Package
    runs-on: macos-latest
    steps:
      - name: 📥 Check out repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: |
          npm install
          cd apps/client && npm install

      - name: 📱 Add iOS Platform & Sync
        run: |
          node apps/client/scripts/build.js
          cd apps/client
          npx --yes cap add ios || true
          npx --yes cap sync ios

      - name: 🔨 Build iOS Project (Unsigned Archive)
        run: |
          cd apps/client/ios/App
          xcodebuild -workspace App.xcworkspace \
                     -scheme App \
                     -configuration Release \
                     -sdk iphoneos \
                     -derivedDataPath build \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGN_IDENTITY="" \
                     CODE_SIGNING_REQUIRED=NO || true

      - name: 📤 Upload iOS Build Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: BlooRush-iOS-App
          path: apps/client/ios/App/build/Build/Products/Release-iphoneos/App.app
          retention-days: 30
